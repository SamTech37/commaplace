# 內容頁改版計畫（2026-05-16）

兩份參考檔：
- `contentpage_ref.html` / `atrium_feed_mockup.html`（同一份 feed 設計）
- `atrium_note_view_bishop.html`（單篇筆記頁設計）

---

# Part A：Feed 頁改版（/feed）

現有 feed 的骨架其實已經跟 mockup 很像（masonry 兩欄、tab、chips、四種卡片變體、卡片底部 meta），所以這次不是大改結構，而是「視覺對齊 + 補一些互動元件」。

## A1. 頂部 navbar
- **現在**：站名、搜尋表單、feed/graph/login 連結各自橫列。
- **參考**：站名 + 一條大的灰底搜尋欄 + 右側一個圓形頭像按鈕（深紫底白字縮寫）。
- **要做**：把搜尋欄改成 nav 內嵌的灰底圓角輸入框，佔中間大半空間；登入後右側顯示圓形頭像縮寫，未登入顯示「登入」連結。

## A2. Tab 列
- **現在**：「推薦 / 追蹤中」兩個。
- **參考**：「推薦 / 追蹤中 / 探索 / 我的」四個。
- **要做**：先加「我的」（連到自己的 profile）；「探索」**先不做**——需要你決定它要顯示什麼。

## A3. 標籤 chips
- 現在有小計數（「#音樂 12」）；參考檔沒有，較乾淨。
- **要做**：保留計數，但縮小、變淡。

## A4. 卡片本體

| 元素 | 現在 | 參考 | 要做 |
|---|---|---|---|
| 內距 | 偏小 | 14×16，呼吸感較好 | 加大內距 |
| 標題行高 | 偏緊 | 1.35 | 微調 |
| 摘要 | 0.82rem | 12.5px 灰色 | 對齊 |
| 引用（quote） | 左邊細線 | 左邊細線 + 襯線字體 | 引用用襯線字體 |
| 連結預覽（links） | 純文字 | 灰底小方塊，逐行列 | 包進淺灰底方塊 |
| #tag | 沒顯示在卡片裡 | 卡片中段一行 `#混音 #workflow` | 在卡片中段補一行 |
| 卡底 stats | `♡ 24　→ 3` | 「連結數」有藍色強調 | 連結數改用主題色 |

## A5. 整體配色與間距
- 邊線統一 0.5px、邊角圓度對齊。
- 現在偏米白；參考檔偏冷灰——**等你決定**。

---

# Part B：單篇筆記頁改版（/{user}/{path}）

這頁差距比 feed 大得多。現在比較像「Markdown 文件 + 下方清單」，參考檔則把它做成「**有作者識別 + 行動按鈕 + 內文 + 結構化的關聯區塊**」的內容頁。

## B1. 頂部 navbar
- 跟 feed 統一：左邊一個「←」回上頁、中間搜尋欄、右邊頭像。
- 取代現在的「← 回 XX」一行 banner。

## B2. 作者橫條（新增）
- **現在**：沒有。作者資訊只夾在標題下方的小字裡（`@handle / 資料夾 · 3 天前`）。
- **參考**：標題上方一條獨立的作者橫條，含：
  - 較大的圓形頭像（40px、配色用作者主題色）
  - 作者名 + 「/ 資料夾」
  - 一行統計：「1,247 訂閱者 · 142 篇筆記 · 24 個資料夾」
  - 右側 **「+ 追蹤」按鈕**（資料庫已有 follows 表，但目前 UI 上看不到追蹤入口；要做）
- **要做**：新增這條 author-bar；訂閱數先用真實資料（追蹤者數、筆記數、資料夾數）。

## B3. 文章 meta 列（改版）
- **現在**：標題下一行小字 + 一列 tag chips。
- **參考**：
  - 左：「更新於 3 天前 · 約 6 分鐘 · #tag #tag #tag」（**閱讀時間是新東西**，用字數估算）
  - 右：三顆動作按鈕——`♡ 89`、**`⬇ 匯入到我的 vault`**（藍底強調）、`⋯`
- **要做**：
  - 補閱讀時間（依字數 / 250 字一分鐘）
  - **「匯入到我的 vault」是新功能**——你要決定：它是「下載 .md」（現在已有），還是「直接複製到我自己的 vault 裡」（這要做後端，等於借用別人的筆記，需要先想清楚怎麼標記來源、是否要徵得作者同意）
  - 「⋯」內含：copy markdown、report、原本的 download .md

## B4. 標題
- **現在**：跟內文同字體大小、無特別處理。
- **參考**：大標 24px、字距微縮、行高 1.3。
- **要做**：放大標題層級。

## B5. 內文 wikilink 樣式
- **現在**：靠 markdown 渲染器產生連結（樣式平淡）。
- **參考**：藍色字 + 藍色虛線底線；hover 時底線變實線、加淺藍底。
- **要做**：在 CSS 對 `.wikilink` class 加上這套樣式。

## B6. 「這篇連到的筆記」區塊（新增 / 取代 local graph）
- **現在**：用 canvas 畫一張小型 local graph。
- **參考**：用**迷你卡片網格**列出這篇連出的筆記，分成兩個子區塊：
  - 同個 vault · 8 篇
  - 跨 vault · 連到其他人的筆記 · 4 篇（每張卡右上角貼「跨 vault」綠色小標籤）
- **要做**：資料來源 `links` 表已有，新增這個區塊。**Local graph 要保留還是換掉？** 兩個都顯示會太擠，建議：
  - 預設顯示**迷你卡片區塊**（資訊密度高、可點開）
  - graph 視覺化收在最上面 navbar 旁邊或 footer 一個按鈕，按了才展開
  - **等你決定**

## B7. 「引用了這篇筆記的人」區塊（backlinks 改版）
- **現在**：兩個小區塊（本 vault / 跨 vault）+ 簡單清單。
- **參考**：合併成一個區塊，每筆是一橫列：「↺ 圖示 + 標題 + 右邊作者小頭像 · 資料夾」。資訊清楚很多。
- **要做**：合併並改版面，保留「本 / 跨 vault」標示但用 badge 或小字。

---

# 實作順序

每一步做完都能單獨看效果。

1. **共用 CSS 基礎**（navbar、頭像、行動按鈕、mini-card）
2. **Feed 卡片內部微調**（內距、引用襯線、連結預覽灰底、補 hashtag、stats 連結數主題色）
3. **Feed navbar 改版**（搜尋欄內嵌 + 頭像）
4. **Feed tab 加「我的」**
5. **筆記頁 navbar + 作者橫條 + 文章 meta 列**
6. **筆記頁 wikilink 樣式 + 標題放大**
7. **筆記頁「這篇連到的筆記」迷你卡片區塊**
8. **筆記頁 backlinks 改版**
9. **chip 計數樣式收尾**

---

# 動工前要你決定的事

1. **配色**：維持你的米白，還是換成參考檔的冷灰？
2. **「探索」tab**：要不要做？做的話顯示什麼？
3. **頭像點開**：只是去個人頁，還是要小選單（個人頁 / 寫筆記 / 登出）？
4. **「匯入到我的 vault」按鈕**：
   - 只是把現有「下載 .md」搬上來改個名？
   - 還是要做新功能（一鍵複製別人的筆記到自己 vault，含來源標記）？這牽涉到資料模型，要先想清楚。
5. **筆記頁的 graph 視覺化**：被迷你卡片區塊取代後，要：
   - 整個拿掉
   - 移到頂部一個按鈕，按了才展開
   - 還是兩個並存（會比較擠）？
6. **追蹤者數、訂閱者數**：作者橫條要顯示三個數字（訂閱、筆記、資料夾）。「訂閱者數」= 追蹤者數，但目前是否有任何頁面顯示這個？要不要保守一點，第一版只顯示「N 篇筆記 · N 個資料夾」？

---

# 不在這次範圍內

- Graph 頁、admin、external、profile 自己的版型：不動。
- 行動裝置：現有是 mobile-first，改完後一樣會在手機上正常顯示。
