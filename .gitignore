bin/
*.db
*.db.*
*.db-[shm|wal|journal]*
.env
.env.*
!.env.example
.session_secret
.DS_Store

# local dev junk and binaries
/tmp
server.log
cookies.txt
*.txt
/import-vault
/commaplace
/commonplace
server
*.log

*-build-plan.md

# Local Claude Code config (private, not shared with teammates)
.claude/*
!.claude/agents
!.claude/skills
!.claude/commands
!.claude/htmx-rules.md
!.claude/budget-render.md
!.claude/specs


# CI workflows must be tracked for GitHub Actions to run them;
# everything else in .github/ stays local. (A bare `.github/` ignore
# can't be negated — git never descends into a fully ignored dir.)
.github/*
!.github/workflows/


# local-only tooling that references personal paths
cmd/import-vault/
cmd/dump-seed/
